line-length = 100
target-version = "py312"

exclude = [
    "venv",
    "migrations",
    "staticfiles",
]

[lint]
select = ["E", "W", "F", "I", "UP", "DJ"]
ignore = [
    "E501",
]

[lint.isort]
known-first-party = ["core", "flashcraft"]

[format]
quote-style = "double"
